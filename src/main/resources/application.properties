spring.application.name=deployment-demo

server.port=8080

management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

app.version=${APP_VERSION:local}
app.branch=${GIT_BRANCH:local}
app.build-number=${BUILD_NUMBER:local}
app.environment=${APP_ENVIRONMENT:local}